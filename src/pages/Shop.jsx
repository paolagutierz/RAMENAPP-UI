import React from "react";
import Page from "../components/Page";
import { Box, Grommet, Footer } from "grommet";

const Cart = (props) => {
  return (
    <Page>
      <Box {...props} />
    </Page>
  );
};

export default Cart;
